def apply(f, x, y, k):
    f(x, y, k)
in
def ackermann(m, n, k):
    if m < 0:
        k(false)
    else:
        if n < 0:
            k(false)
        else:
            if m == 0:
                k(n + 1)
            else:
                if n == 0:
                    ackermann(m - 1, 1, k)
                else:
                    ackermann(m, n - 1, lambda r: ackermann(m - 1, r, k) end)
in
print(apply(ackermann, 2, 2, lambda x: x end)) == 7

